# shellcheck shell=bash

SECURSOR_ROOT=$(cd "$(dirname "${BASH_SOURCE[0]:-$0}")" && pwd -P)

[[ :$PATH: == *:"$SECURSOR_ROOT/bin":* ]] ||
  PATH=$SECURSOR_ROOT/bin:$PATH

export SECURSOR_ROOT PATH

# vim: ft=sh:
